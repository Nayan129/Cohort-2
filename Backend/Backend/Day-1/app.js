// we required express package and store it inside express variable
const express = require("express");
// we store express inside app
const app = express();
// running app on a particular port i.e. 3000
app.listen(3000);
